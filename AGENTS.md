# Agent Instructions

Use `.agents/` as the source of truth for project AI guidance,
workflows, and shared conventions. Start with `.agents/README.md`.

Before making changes, inspect the relevant files under `.agents/` and
follow the guidance that applies to the task.

Keep tool-specific adapters thin. Put reusable workflow steps, coding
guidance, and project conventions under `.agents/`.
