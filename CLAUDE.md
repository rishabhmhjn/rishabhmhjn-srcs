# Claude Instructions

Use `.agents/` as the source of truth for project AI guidance,
workflows, and shared conventions. Start with `.agents/README.md`.

Before proposing or making changes, discover and follow the relevant
guidance under `.agents/`.

Keep this file as a thin Claude adapter. Put reusable workflow bodies,
project conventions, and future skill inventories under `.agents/`.
